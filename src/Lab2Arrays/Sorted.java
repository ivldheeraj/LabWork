package com.cg.arrayslabwork;
import java.util.Arrays;

public class Sorted {

	public int [] sortArray(int[] numbers) {
		if(numbers.length==0) {
			return numbers;
		}
		else if(numbers.length==1) {
			return numbers;
		}
		return numbers;
	}
}

